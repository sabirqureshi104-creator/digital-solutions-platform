# Milestone 3 — Real Authentication

1. Import `database/auth_schema.sql` into the `digital_solutions` database.
2. Confirm database values in `app/Config.php`.
3. Open `/admin/create-admin.php`.
4. Create the first admin account.
5. Delete `admin/create-admin.php`.
6. Sign in at `/admin/login.php`.
